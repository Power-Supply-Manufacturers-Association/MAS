{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://psma.com/mas/magnetic/wire/round.json",
    "$comment": "SPDX-License-Identifier: Apache-2.0",
    "$ref": "./basicWire.json",
    "title": "wire/round",
    "description": "The description of a solid round magnet wire",
    "type": "object",
    "properties": {
        "type": {
            "$ref": "../wire.json#/$defs/wireType",
            "const": "round",
            "$comment": "Discriminator pin with the shared enum type: the $ref keeps code generators (quicktype -> MAS.hpp WireType) on the common enum; the const sibling narrows validation to this subtype only (same semantics as a bare const)."
        },
        "material": {
            "title": "WireMaterialDataOrNameUnion",
            "oneOf": [
                {
                    "$ref": "./material.json"
                },
                {
                    "description": "The name of the material used in the wire",
                    "type": "string"
                }
            ]
        },
        "conductingDiameter": {
            "description": "The conducting diameter of the wire, in m",
            "$ref": "../../utils.json#/$defs/dimensionWithTolerance"
        },
        "outerDiameter": {
            "description": "The outer diameter of the wire, in m",
            "$ref": "../../utils.json#/$defs/dimensionWithTolerance"
        }
    },
    "required": [
        "type",
        "conductingDiameter"
    ],
    "$defs": {},
    "unevaluatedProperties": false
}
