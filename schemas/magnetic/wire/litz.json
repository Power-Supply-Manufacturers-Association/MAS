{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://psma.com/mas/magnetic/wire/litz.json",
    "$comment": "SPDX-License-Identifier: Apache-2.0",
    "$ref": "./basicWire.json",
    "title": "wire/litz",
    "description": "The description of a stranded litz magnet wire",
    "type": "object",
    "properties": {
        "type": {
            "$ref": "../wire.json#/$defs/wireType",
            "const": "litz",
            "$comment": "Discriminator pin with the shared enum type: the $ref keeps code generators (quicktype -> MAS.hpp WireType) on the common enum; the const sibling narrows validation to this subtype only (same semantics as a bare const)."
        },
        "strand": {
            "description": "The wire used as strands",
            "oneOf": [
                {
                    "title": "wireRound",
                    "$ref": "./round.json"
                },
                {
                    "description": "The name of the wire used as strand",
                    "type": "string"
                }
            ]
        },
        "outerDiameter": {
            "description": "The outer diameter of the wire. Unit: m.",
            "$ref": "../../utils.json#/$defs/dimensionWithTolerance"
        },
        "twistPitch": {
            "description": "Length of one full twist of the litz bundle along the wire axis. Unit: m. Vendors quote this in mm or as 'lay length'; convert to metres before serialisation. No international standard exists for litz construction; this field captures the most commonly published vendor parameter.",
            "$ref": "../../utils.json#/$defs/dimensionWithTolerance"
        }
    },
    "required": [
        "type",
        "strand",
        "outerDiameter"
    ],
    "unevaluatedProperties": false
}
